# Инструкция по партнёрству — модель дропшиппинга (15.09.2026)

1. Поле `dropship` + `dropshipPriority` в docs/outreach/data/*.json (схема _schema.md).
2. 6 агентов — проверка 257 партнёров на дропшиппинг и добор дропшип-поставщиков; 1 агент — схема B2B-дропшиппинга в РФ.
3. Шаблон письма «дропшиппинг», процесс, схема документов и денег, чек-лист договора.
4. PDF + трекер на рабочий стол, отчёт docs/reports/2026-09-15-dropshipping.md.
