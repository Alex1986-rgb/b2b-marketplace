# План: из макета — полноценный сайт (14.09.2026)

Эталон экранов — `docs/spec-screens.md`. План визуала — `docs/visual-plan.json` / `.md`.

1. **Анализ всех 38 экранов** — автопрогон (ошибки, переполнение, заглушки) + 4 визуальных ревью + ревью контента.
2. **Правки макета** — пачки `tools/fix_batch1..4.py`: арифметика, сквозные цифры, юр. риски, шапки кабинетов, адаптив, шрифты Fira Sans (с кириллицей).
3. **Визуал** — 13 новых фото (`tools/gen_photos.py`, Arion HUB), 138 иконок (`tools/make_icons.js`: Lucide + 16 своих), знак и фавиконы (`tools/make_brand.js`), расстановка — `tools/fix_batch5.py`.
4. **Сборка сайта** — `tools/build_site.js`: у каждого экрана свой адрес (`tools/routes.js`), кнопки-переходы → ссылки, title/description/canonical/OG/JSON-LD, sitemap, robots, 404, типографика, `site.js` (демо-действия, поиск, корзина, карусель).
5. **Проверка** — десктоп и 390 px, ссылки, консоль.
6. **Публикация** — GitHub Pages из `site/`, отчёт в `docs/reports/`.
