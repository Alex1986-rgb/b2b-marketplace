# Доступность (WCAG 2.2 AA) и согласованность данных — 14.09.2026

Проверялась сборка от 14.09 18:42 (135 страниц, товары, статьи, направления и бренды собраны из `tools/data/*.json`). В репозитории ничего не менялось, это только отчёт.

**Как проверяли.** axe-core 4.x через puppeteer-core, наборы wcag2a/aa, 21a/aa, 22aa и best-practice. 9 страниц, ширина 1440 и 390. Страницы: главная, `katalog/nasosy/`, карточка из макета `grundfos-cr-32-4`, сгенерированные `grundfos-cr-45-3` и `wilo-helix-v-3604`, `korzina/`, форма КП `kaishan-lgcy-75`, `blog/`, `kontakty/`. Вручную: обход Tab, меню на 390, FAQ, фильтры, контраст на тёмном hero (попиксельно по скриншотам), подписи полей, фото, заголовки. Данные сверялись сквозным сканером по всем 136 HTML-файлам: 346 упоминаний товаров сравнивались с `products.json`.

## Сводка

| | P1 | P2 | P3 |
|---|---|---|---|
| Доступность | 5 | 9 | 6 |
| Данные | 3 | 5 | 4 |

Что уже хорошо: `lang="ru"` на всех страницах. Видимый фокус есть везде (2px, акцентный цвет; на тёмном фоне контраст 3,4:1, это проходит по 1.4.11). Меню на 390 открывается с клавиатуры, держит фокус, закрывается по Esc и возвращает фокус на кнопку. FAQ `<details>` открывается Enter/Space. Фильтры доступны с клавиатуры (Space переключает, `aria-checked` обновляется), у чипов есть «Снять фильтр: …». Тосты объявляются через `role=status`. У количества в корзине есть `aria-label`. На тёмном hero текст читается хорошо: заголовок и подзаголовок 12–14:1, фото уходит в градиент и под текстом не лежит. На duotone-фото текста нет, только белые плашки «N шт. в наличии» (≥16:1).

---

## Часть 1. Доступность

### P1: блокирует пользователя или прямо нарушает AA

| # | Где | Проблема (критерий) | Правка |
|---|---|---|---|
| A1 | Все страницы, 390 | У кнопки «Меню» (`.pk-menu-btn`) нет имени: подпись скрыта через `display:none` (4.1.2). axe: button-name, critical | JS-1 |
| A2 | Все страницы, 390 | Кнопка «Найти» в шапке и в hero без имени: `.hide-m` прячет текст, остаётся иконка (4.1.2) | JS-1 |
| A3 | `korzina/`, форма КП `kaishan-lgcy-75` (и все формы на `.field`) | `<label>` не связан с полем: нет `for`/`id`. Имя берётся из placeholder, поэтому поле «Телефон» читается как «+7», «Почта» как «zakupki@» (1.3.1, 3.3.2, 4.1.2) | JS-2 |
| A4 | Карточка `grundfos-cr-32-4` (макет) | Степпер количества `− 1 +` сделан из `<span>`: мышью работает, с клавиатуры до него не добраться (2.1.1) | JS-3 |
| A5 | Контраст, все страницы | Основные цвета не проходят 4,5:1: `--color-neutral-600 #7a7a7d` на белом 4,28, на фоне `#f4f5f7` 3,92 (≈240 узлов: счётчики каталога, крошки, подписи). Акцентный текст `#5980a6` на белом 4,14, на фоне 3,80 («все 45 разделов», «Добавить свой прайс», номера шагов). Белый текст на кнопках `.btn-primary` `#5980a6` 4,14 («Найти», «В корзину», «Отправить заявку»). `.text-muted #838484` 3,75 (итоги корзины). `th #777979` 4,38. Счётчики в шапке: белый 10px на акценте, 4,15 (1.4.3) | CSS-1 |

### P2

| # | Где | Проблема (критерий) | Правка |
|---|---|---|---|
| A6 | Страницы из макета (главная, каталог, карточка, корзина, КП, блог) | Нет `<main>`: axe landmark-one-main, region ×18–55. На сгенерированных страницах `<main>` есть | BUILD-1 |
| A7 | Все страницы | Нет ссылки «К содержимому»: до контента 23 остановки Tab (2.4.1) | BUILD-2 + CSS-2 |
| A8 | Главная, карточка из макета, корзина, КП | Сразу после `h1` идёт `h3`, `h2` пропущен («Часто заказывают», «Характеристики», «Оформление», «Что входит в предложение») (1.3.1) | BUILD-3 |
| A9 | Шапка, все страницы | Поле поиска подписано только placeholder'ом. `select` сортировки (`katalog/nasosy/`) и рубрик (`blog/`) без имени (axe select-name, critical). Поля цены «от 12 000 / до 900 000» подписаны тоже только placeholder'ом (3.3.2, 4.1.2) | JS-1 |
| A10 | Фото товаров | Главное фото карточки (`figure[data-photo]`) и миниатюры галереи сделаны фоновыми картинками без `role="img"`/`aria-label`: для скринридера фото нет (1.1.1). Фото в карточках внутри ссылок декоративные, но не скрыты | JS-4 (сразу) + GEN-1 (в генераторе) |
| A11 | Форма КП, корзина | Нет `autocomplete` у полей «Контактное лицо / Телефон / Почта / Компания / Адрес» (1.3.5). Телефон и почта `type=text` | JS-2 |
| A12 | `katalog/nasosy/` | «Найдено N» меняется при фильтрации, но не объявляется (4.1.3). Группы фильтров не размечены как группы («Производитель», «Подача») (1.3.1) | JS-5 |
| A13 | Главная | Бегущая строка брендов движется дольше 5 секунд, остановить её нельзя (2.2.2). `prefers-reduced-motion` учтён, но этого мало | CSS-3 |
| A14 | `korzina/`, 390 | Кнопки `−`/`+` по площади меньше 24×24 (2.5.8, axe target-size) | CSS-4 |

### P3

| # | Где | Проблема | Правка |
|---|---|---|---|
| A15 | Главная | Пустые `<th>` в таблице «Часто заказывают» (колонки фото и кнопки) | BUILD-4 |
| A16 | Шапка | Два `nav` без различающих имён (landmark-unique) | BUILD-4 |
| A17 | Шапка, 390 | Имена иконок-ссылок звучат как «3Запросы КП», «3Корзина» | JS-1 |
| A18 | `katalog/nasosy/` | `role="checkbox"` на `<label>` (axe aria-allowed-role). С клавиатуры работает, но надёжнее перенести роль на `span`-квадрат или взять настоящий `input` | не срочно |
| A19 | Карточка из макета | Звёзды рейтинга (`#e0a526`, 1,5–2:1) скринридер зачитывает символами «★★★★★». Числовая оценка «4,7» рядом есть | JS-4 (aria-hidden звёздам) |
| A20 | Формы | Обязательные поля не отмечены: валидатор требует заполнить все (3.3.2) | в разметку макета: `*` + `aria-required` |

### Готовые правки

**CSS-1…4: дописать в конец `tools/site/skin.css`**

```css
/* ══ Доступность: контраст, пропуск навигации, бегущая строка, цели касания (a11y 14.09.26) ══ */
:root {
  --color-neutral-600: #6b6b6e;   /* было #7a7a7d: 5,31 на белом, 4,87 на #f4f5f7 */
  --pk-accent-ink: #4a6f94;       /* акцент для текста и заливок под белым текстом: 5,26 / 4,82 */
}
.text-muted, .table th { color: #6b6b6e !important; }
/* акцентный текст-ссылки и кикеры */
[style*="color: var(--color-accent)"]:not(.ico):not([class*="i-"]),
.card-kicker, a[style*="color: var(--color-accent)"] { color: var(--pk-accent-ink) !important; }
/* кнопки с белым текстом */
.btn-primary { background-color: var(--pk-accent-ink) !important; border-color: var(--pk-accent-ink) !important; color: #fff !important; }
.btn-primary:hover { background-color: var(--color-accent-700) !important; border-color: var(--color-accent-700) !important; }
/* счётчики в шапке */
header span.mono[style*="position: absolute"] { background: var(--pk-accent-ink) !important; color: #fff !important; }

/* CSS-2: ссылка «К содержимому» и визуально скрытый текст */
.pk-skip { position: absolute; left: 12px; top: -60px; z-index: 1100; background: #fff; color: var(--color-text);
  padding: 10px 14px; border-radius: 8px; box-shadow: 0 4px 18px rgba(0,0,0,.2); text-decoration: none; font-weight: 600; }
.pk-skip:focus { top: 12px; }
.pk-vh { position: absolute !important; width: 1px; height: 1px; margin: -1px; overflow: hidden; clip: rect(0 0 0 0); white-space: nowrap; border: 0; }

/* CSS-3: бегущая строка останавливается при наведении/фокусе и по кнопке (кнопку ставит JS-5) */
.marquee-wrap:hover .marquee-track, .marquee-wrap:focus-within .marquee-track,
.marquee-wrap.pk-paused .marquee-track { animation-play-state: paused !important; }
.pk-marquee-toggle { font-size: 12px; min-height: 28px; padding: 2px 10px; border-radius: 999px; border: 1px solid var(--line); background: #fff; cursor: pointer; }

/* CSS-4: степпер в корзине не меньше 32×32 */
.cartrow .seg > button.seg-opt, .seg > [role="button"].seg-opt { min-width: 32px; min-height: 32px; }
```

> Проверить после сборки: `--pk-accent-ink` темнее исходного акцента. Бордеры, фокус и иконки остаются на `--color-accent`, перекрашиваются только текст и заливки под белым текстом. Если перекраска кнопок не понравится по визуалу, можно оставить `#5980a6` и сделать текст кнопок 18,66px bold: крупному тексту хватает 3:1.

**JS-1…5: `tools/site/site.js`, перед блоком `// ── старт ──`, вызов `initA11y();` поставить после `initMenu();`**

```js
  // ═════════ Доступность (a11y 14.09.26) ═════════
  function initA11y() {
    // JS-1: имена кнопок-иконок и полей без подписи
    if (menuBtn) menuBtn.setAttribute('aria-label', 'Меню');
    $$('.hide-m').forEach(function (s) { var b = s.closest('button, a'); if (b && !b.getAttribute('aria-label')) b.setAttribute('aria-label', txt(s)); });
    $$('button, a').forEach(function (b) { if (!txt(b) && !b.getAttribute('aria-label') && $('[class*="i-ui-search"]', b)) b.setAttribute('aria-label', 'Найти'); });
    $$('input.input').forEach(function (i) {
      if (i.getAttribute('aria-label') || i.id && $('label[for="' + i.id + '"]')) return;
      var p = i.placeholder || '';
      if (/модель|артикул|название/i.test(p)) i.setAttribute('aria-label', 'Поиск по модели, артикулу или названию');
      else if (/^от\s/i.test(p)) i.setAttribute('aria-label', 'Цена от, ₽');
      else if (/^до\s/i.test(p)) i.setAttribute('aria-label', 'Цена до, ₽');
    });
    $$('select').forEach(function (s) {
      if (s.getAttribute('aria-label')) return;
      s.setAttribute('aria-label', /дешёв|релевант/i.test(txt(s)) ? 'Сортировка' : /Все направления/i.test(txt(s)) ? 'Рубрика' : 'Выбор');
    });
    $$('header a').forEach(function (a) { // «3Корзина» → «Корзина: 3»
      var n = $('span.mono[style*="position: absolute"]', a); if (!n) return;
      var name = txt(a).replace(txt(n), '').trim(); if (name) a.setAttribute('aria-label', name + ': ' + txt(n));
    });

    // JS-2: label ↔ поле и autocomplete
    var AC = [[/телефон/i, 'tel', 'tel'], [/почт|e-?mail/i, 'email', 'email'], [/контакт|имя|лицо/i, 'name'], [/компани|плательщик|организац/i, 'organization'], [/адрес/i, 'street-address'], [/инн/i, 'off']];
    $$('.field').forEach(function (f, i) {
      var l = $('label', f), c = $('input, select, textarea', f); if (!l || !c) return;
      if (!c.id) c.id = 'pk-f' + i;
      if (l.tagName === 'LABEL') l.htmlFor = c.id; else { l.id = l.id || 'pk-l' + i; c.setAttribute('aria-labelledby', l.id); }
      var t = txt(l);
      for (var k = 0; k < AC.length; k++) if (AC[k][0].test(t)) { c.setAttribute('autocomplete', AC[k][1]); if (AC[k][2] && c.tagName === 'INPUT') c.type = AC[k][2]; break; }
      if (/инн/i.test(t)) c.setAttribute('inputmode', 'numeric');
    });

    // JS-3: степпер карточки товара — клавиатура
    $$('.seg > span.seg-opt').forEach(function (s) {
      var v = txt(s); if (s.closest('.cartrow')) return;
      if (v === '+' || v === '−') {
        s.setAttribute('role', 'button'); s.setAttribute('tabindex', '0');
        s.setAttribute('aria-label', v === '+' ? 'Увеличить количество' : 'Уменьшить количество');
        s.addEventListener('keydown', function (e) { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); s.click(); } });
      } else if (/^\d+$/.test(v)) { s.setAttribute('role', 'status'); s.setAttribute('aria-live', 'polite'); s.setAttribute('aria-label', 'Количество'); }
    });

    // JS-4: фото — информативные получают имя, декоративные скрываются
    var h1 = txt($('h1'));
    $$('[data-photo], .gal > span').forEach(function (el, i) {
      if (el.getAttribute('role') === 'img' || el.hasAttribute('aria-hidden')) return;
      var inLink = el.closest('a, [data-href]'), main = el.matches('figure') || el.closest('.split > :first-child');
      if (main && !inLink) {
        el.setAttribute('role', 'img');
        el.setAttribute('aria-label', h1 + (el.closest('.gal') ? ' — фото ' + ([].indexOf.call(el.parentElement.children, el) + 1) : ' — фото'));
      } else el.setAttribute('aria-hidden', 'true');
    });
    $$('.gal[role="list"] > span[role="img"]').forEach(function (s) { var w = document.createElement('span'); w.setAttribute('role', 'listitem'); s.parentElement.insertBefore(w, s); w.appendChild(s); });
    $$('span').forEach(function (s) { if (/^★+$/.test(txt(s))) s.setAttribute('aria-hidden', 'true'); });

    // JS-5: фильтры и бегущая строка
    if (filters) {
      if (filters.found) { filters.found.setAttribute('role', 'status'); filters.found.setAttribute('aria-live', 'polite'); }
      filters.boxes.forEach(function (l) { var g = l.parentElement, t = $('span.mono', g); if (t && !g.getAttribute('role')) { t.id = t.id || 'pk-g' + Math.random().toString(36).slice(2, 7); g.setAttribute('role', 'group'); g.setAttribute('aria-labelledby', t.id); } });
    }
    $$('.marquee-wrap').forEach(function (w) {
      var b = document.createElement('button'); b.type = 'button'; b.className = 'pk-marquee-toggle'; b.textContent = 'Пауза';
      b.setAttribute('aria-pressed', 'false');
      b.addEventListener('click', function () { var on = w.classList.toggle('pk-paused'); b.textContent = on ? 'Продолжить' : 'Пауза'; b.setAttribute('aria-pressed', String(on)); });
      w.insertAdjacentElement('beforebegin', b);
    });
  }
```

> JS-правки работают сразу после загрузки страницы. Постоянное решение для A3/A10: писать атрибуты в разметку при сборке (BUILD и GEN ниже), тогда страницы корректны и без JS. Проверить, что делегированный обработчик кликов `.seg > span.seg-opt` срабатывает на `s.click()`: он висит на `document`, должен сработать.

**BUILD-1…4: `tools/build_site.js`, внутри `inPage()`, перед блоком `// типографика` (разметка правится один раз при сборке)**

```js
  // ── доступность ──
  // BUILD-1: основная область. Если футер оказался внутри экрана, main ставим на обёртку без футера
  const scr = c.querySelector('[data-screen-label]');
  if (scr && !c.querySelector('main')) {
    if (!scr.querySelector('footer')) { scr.setAttribute('role', 'main'); scr.id = 'main'; }
    else { const m = document.createElement('main'); m.id = 'main'; const f = scr.querySelector(':scope > footer'); [...scr.childNodes].filter(n => n !== f && !(n.nodeType === 1 && n.matches('header'))).forEach(n => m.appendChild(n)); scr.insertBefore(m, f); }
  }
  // BUILD-3: без скачков уровней заголовков — семантика через aria-level, вёрстку не трогаем
  { let last = 1; for (const h of c.querySelectorAll('h1,h2,h3,h4,h5,h6')) { const lv = +h.tagName[1]; if (lv > last + 1) { h.setAttribute('role', 'heading'); h.setAttribute('aria-level', String(last + 1)); last += 1; } else last = lv; } }
  // BUILD-4: пустые заголовки таблиц и имена навигаций
  for (const th of c.querySelectorAll('th')) if (!th.textContent.trim()) th.innerHTML = '<span class="pk-vh">' + (th.closest('thead') && [...th.parentElement.children].indexOf(th) === th.parentElement.children.length - 1 ? 'Действие' : 'Фото') + '</span>';
  const hn = c.querySelector('header nav'); if (hn && !hn.getAttribute('aria-label')) hn.setAttribute('aria-label', 'Разделы сайта');
  for (const f of c.querySelectorAll('figure[data-photo]')) { f.setAttribute('role', 'img'); f.setAttribute('aria-label', (c.querySelector('h1') || {}).textContent.trim() + ' — фото'); }
```

BUILD-2: в `page()` сразу после `<body>`:

```js
<body>
<a class="pk-skip" href="#main">К содержимому</a>
${body}
```

В `gen_pages.js` у `wrap` добавить `id="main"`: `` const wrap = inner => `<main id="main" style="…">${inner}</main>` ``. Так же во втором `wrap` в `build_site.js` (служебные страницы) и в `<main>` страницы 404.

**GEN-1: `tools/gen_pages.js`, фото с альтернативным текстом**

```js
const img = (id, h, fit = 'cover', extra = '', alt = '') => `<div class="ph${fit === 'cover' ? ' duotone' : ''}" data-photo="1" ${alt ? `role="img" aria-label="${esc(alt)}"` : 'aria-hidden="true"'} style="height:${h}px;background:url(IMGBASE${id}.jpg) center/${fit} no-repeat ${fit === 'contain' ? '#fff' : ''};${extra}"></div>`;
```

Вызовы:
- главное фото товара: `img(photos[0], 380, 'contain', 'border-radius:14px;…', (p.fullName || p.name) + ' — фото')`;
- галерея: `<div class="gal" role="list" aria-label="Фото товара" …>` и каждый элемент `<span role="listitem"><span role="img" aria-label="${esc(p.name)} — фото ${i + 1}" style="…"></span></span>`;
- обложка статьи: вместо `div.duotone.ph` вызвать с `role="img" aria-label="${esc(a.title)}"`;
- обложка направления и фото в `productCard`/`articleCard` остаются декоративными (`aria-hidden`, alt не передаётся).

---

## Часть 2. Согласованность данных

### Цены, наличие и артикулы (эталон: `tools/data/products.json`)

Проверено у всех 24 товаров. Цена, артикул, `title` и цена в JSON-LD на странице товара совпадают с JSON. В каталоге, на главной, в поиске, поиске по фото, чате, корзине, заявке списком, блоге, у брендов и направлений цены совпадают. Суммы строк сходятся: корзина 299 100 ₽ (2×104 900 + 50×540 + 62 300), спецификация в чате 168 640 ₽, заказ ПК-10428 222 600 ₽. Характеристики насосов (подача, напор, мощность) в чате, сравнении и таблице серии совпадают с `specs`.

| P | Где | Значение там | Где ещё | Значение тут | Какое верное |
|---|---|---|---|---|---|
| **P1** | `sravnenie/`, колонка Grundfos CR 45-3, «Наличие» и «Отгрузка» | **3 шт.**, **3 дня** | `products.json`, страница товара, `katalog/nasosy/` (макет), `chat/` | **5 шт. в наличии**, **2–4 дня** | 5 шт., 2–4 дня (так в каталоге макета; сравнение в макете разошлось с ним) |
| **P1** | `katalog/kompressory/kaishan-lgcy-75/` (макет), `zayavka-spiskom/` | артикул **KSN-LGCY-75-10** | `products.json`, главная «Часто заказывают» | **KSN-LGCY-75** | в макете встречаются оба варианта (по 2 раза). Предлагаю KSN-LGCY-75-10: он на самой карточке и указывает исполнение на 10 бар. Поправить `sku` в JSON и таблицу на главной |
| **P1** | `katalog/podshipniki/skf-6205-2rs/`, JSON-LD | `availability: PreOrder` | та же страница, текст | «1 400 шт.» (в наличии) | InStock. Регулярка в `gen_pages.js` ищет «налич», а у SKF в `stock` просто «1 400 шт.». Правка: `availability: /налич\|\d\s*шт/i.test(p.stock \|\| '') ? '…/InStock' : /уточня/i.test(p.stock \|\| '') ? '…/LimitedAvailability' : '…/PreOrder'` (у виброопоры «Наличие уточняется») |
| P2 | `zayavka-spiskom/`, «Задвижка DN100 PN16», 3 шт. | **58 400 ₽** | `products.json` / страница товара | 19 470 ₽ × 3 = **58 410 ₽** | в макете только сумма 58 400. Цена за штуку 19 466,67 не целая, поэтому в JSON округлено до 19 470. Верно: оставить 19 470 и в заявке списком написать **58 410 ₽** |
| P2 | `katalog/nasosy/` (макет): фильтр Grundfos + 20–60 м³/ч + «В наличии» | «Найдено **1 128**» | `proizvoditeli/grundfos/` (макет) | в наличии всего **214** позиций Grundfos (из 1 440) | в наличии с фильтром не может быть больше 214. Нужно одно число ≤ 214, например «Найдено 86» |
| P2 | `katalog/nasosy/`: фильтр производителей Wilo/CNP/Ebara → «Найдено 0» (логика в `site.js`) | 0 позиций | `products.json`, `proizvoditeli/wilo/`, `…/cnp/` | есть Wilo Helix V 3604/4204, CNP CDLF/NIS, Ebara 3M | в выдачу каталога добавить эти 6 карточек из JSON (генерировать сетку из `products.json`) либо убрать правило «не Grundfos → пусто» |
| P2 | `tools/data/brands.json`, grundfos `series` | CR 240 от 58 000 ₽, NB/NK 160, TP 130, MAGNA3 70, SP 110, SEG/SL 60; Σ 770 | `proizvoditeli/grundfos/` (макет) | CR/CRN 420 от 48 700, NB/NK 310 от 96 200, MAGNA3 180 от 62 400, SP 240 от 74 100, DDA/DDI 160 от 82 900, SEG/SE 130 от 118 600; Σ 1 440 | макет. Сейчас страница Grundfos не генерируется (`existing`), но JSON устарел, и при переходе на генерацию цифры разъедутся |
| P2 | `katalog/nasosy/grundfos-cr-32-4/` (макет), блок покупки | «8 шт. **на складе**», «2–4 **рабочих дня**», «Гарантия 24 **мес.**» | все остальные места и 23 сгенерированные карточки | «8 шт. в наличии», «2–4 дня», «24 месяца» | одна формулировка на весь сайт. Предлагаю «в наличии» и «2–4 дня». Сейчас `site.js` определяет наличие по регулярке «налич», поэтому «на складе» — ещё и потенциальный баг |
| P3 | `proizvoditeli/nord/`, серия NORDBLOC.1 | «от 58 000 ₽» (самая дешёвая серия: UNIVERSAL от 32 000) | `katalog/privod/nord-sk-02/` | Мотор-редуктор NORD SK 02, **48 200 ₽** | SK 02 относится к NORDBLOC.1: либо «от 48 200», либо указать серию SK 02 в карточке. Проверить по прайсу |
| P3 | `korzina/`, «в т. ч. НДС 22%» | 53 937 ₽ | `site.js paintTotals` после любой правки корзины | 53 938 ₽ (`Math.round`) | 299 100 × 22/122 = 53 937,7 → **53 938 ₽**. Поправить статичную цифру в макете |
| P3 | `napravleniya/nasosy/`, `katalog/nasosy/` | «4 218 моделей **от 37 производителей**» | `brands.json` / блок «Производители» | 16 брендов в данных, 12 в списке на странице | не ошибка, но 37 ничем не подтверждается. Либо «более 30 производителей», либо список «и ещё 25» |
| P3 | Главная, раздел «Каталог» | «115 000+ моделей» | Σ `directions[].count` | 117 108 | сходится («более 115 000»). Отмечено для порядка |

Направления: у всех 45 число позиций совпадает на `napravleniya/`, на главной и на своей странице. Суммы подкатегорий совпадают с `count`. Бренды: в сгенерированных карточках «Модели в каталоге» те же цены, что в JSON. Цены «от» у серий не выше реальных цен товаров, кроме NORD (выше).

### «Внутренняя кухня» на публичных страницах

Вне разрешённых «О сервисе» и «Поставщикам» наценки, маржи, закупочных цен и «Поставщик А/Б/В» на страницах витрины, в каталоге, блоге, у брендов и направлений нет. Найденные «разница в цене» и «себестоимость детали» в статьях — клиентский контекст, это нормально.

| P | Где | Что видно | Рекомендация |
|---|---|---|---|
| **P2** | `kabinet/zakaz-pk-10428/` (кабинет клиента) | колонка «Поставщик»: **Гидромаш**, **ПромАрматура**; статус «Передан поставщикам Гидромаш и ПромАрматура» | клиенту раскрыты реальные источники. `o-servise` при этом отвечает на вопрос «Можно ли купить дешевле напрямую у поставщика?». Заменить на «Склад поставщика · Москва» / «Отгрузка 1 из 2» |
| P2 | `panel/admin`, `avtopilot`, `crm`, `ekonomika`, `prava`, `oshibki`, `logistika`, `klient` | базовая наценка 20→18 %, маржа 19,2 %, «цена ниже закупочной», торг «маржа позволяет −4 %», фиды поставщиков | это внутренние экраны по замыслу: `noindex`, в sitemap нет, публичные страницы на них не ссылаются. Но на GitHub Pages они открываются по прямому адресу без входа. Минимум — `Disallow: /b2b-marketplace/panel/` в `robots.txt` (генерируется в `build_site.js`) и плашка «Демо внутренней панели, данные вымышлены». Надёжнее убрать `panel/` из публичной сборки |
| P3 | `kabinet-postavshchika/` | «ООО «Гидромаш»», фид, прайс и остатки | кабинет самого поставщика, его собственные данные. Допустимо, `noindex` стоит |

---

## Что осталось / что нужно от Александра

- **Решить:** какой артикул у Kaishan (`KSN-LGCY-75-10` или `KSN-LGCY-75`), какое число ставить в «Найдено» под фильтром (≤ 214) и оставлять ли `panel/` в публичной сборке.
- **По визуалу:** согласовать более тёмный акцент `#4a6f94` для кнопок и текстовых ссылок. Это главный источник нарушений контраста, около 70 узлов на страницу.
- **Не проверено:** страницы вне списка (чат, поиск, сравнение, кабинеты и панели) axe'ом не прогонялись, по ним сверялись только данные. Не проверялось со скринридером (VoiceOver) и при зуме 400 % (1.4.10).
- После правок перезапустить проверку (скрипты во временной папке сессии: `a11y/axe.js`, `manual.js`, `kb.js`, `dp.js`, `data.js`). Ориентир: critical = 0, contrast ≤ 5 на страницу.

## Файлы

- Отчёт: `/Users/alexandr/projects/b2b-marketplace/docs/reports/2026-09-14-a11y-data.md`
- Куда вносить правки: `tools/site/skin.css`, `tools/site/site.js`, `tools/build_site.js`, `tools/gen_pages.js`, `tools/data/products.json`, `tools/data/brands.json`, макет `project/Промышленный агрегатор.dc.html` (сравнение, заявка списком, фильтр каталога, заказ ПК-10428, карточка CR 32-4)
