# Итог: ПРОМКОНТУР — из макета в полноценный сайт (14.09.2026)

Живой адрес: https://alex1986-rgb.github.io/b2b-marketplace/ · репозиторий: https://github.com/Alex1986-rgb/b2b-marketplace

## Что сделано
- **135 страниц** вместо одного файла-макета: 35 экранов витрины, кабинетов и панели оператора + сгенерированные из данных
  24 товара, 15 статей, 44 направления, 15 производителей; служебные — контакты, карта сайта, политика, условия, 404.
- **Каждая карточка открывает свою страницу** (товары, статьи, направления, бренды). Проверено обходчиком `tools/qa/crawl_links.js`:
  битых ссылок 0, клик-тесты каталога/блога/направлений на 390 и 1440 — OK.
- **Вид настоящего сайта**: слой `tools/site/skin.css` (карточки с тенью и скруглением, пилюли, чат-пузыри, степпер заказа,
  прилипающая шапка), шрифты Fira Sans с кириллицей, 138 иконок (Lucide + 16 своих), знак и фавиконы,
  знаки брендов в бегущей строке (без копирования чужих логотипов).
- **Фото**: 45 кадров (сцены с синей тонировкой, товары на белом), WebP + ленивая подгрузка; страницы легче в 2–2,4 раза.
- **Интерактив без бэкенда** (`tools/site/site.js`): корзина с пересчётом и НДС 22 %, фильтры и сортировка каталога,
  формы с проверкой и экраном «принято (демо)», мобильное меню, карусели, поиск.
- **Телефон**: шапка ~150 px, цена товара сразу после фото, таблицы-карточки, фильтры, кабинет-лента.
- **SEO**: title/description всех публичных страниц, canonical, OG, JSON-LD (Organization, BreadcrumbList, Product, Article, FAQPage),
  sitemap (118 адресов), robots, noindex у кабинетов и панели.
- **Доступность**: контраст (акцент #4a6f94), «К содержимому», подписи полей и кнопок-иконок, клавиатурный степпер, пауза бегущей строки.
- **Контент и данные**: ~400 правок цифр и текстов по ТЗ владельца (`docs/spec-screens.md`), убраны юр. риски
  («официальные поставки»), наценка и закупочные цены убраны с главной.

## Дополнено (этап 2)
- **Поиск по сайту** (`tools/site/search.js`, индекс `site/search-index.json`): подсказки в шапке и hero, страница результатов
  `poisk/?q=`, опечатки, транслит (грундфос → Grundfos), неверная раскладка (yfcjc → насос), поиск по артикулу.
- **Хаб производителей** `proizvoditeli/` (16 брендов по профилям), крошки и подвал ведут на него.
- **Блог**: все 16 статей — блок «Ещё статьи» под лентой из макета.
- **Свои фото** у Grundfos CR 32-2, 32-6, 45-3, 20-5 и виброопор.

## Дополнено (этап 3, 15.09.2026) — кабинеты, админка, SEO на всех страницах
- **194 страницы**: 123 публичные + 71 закрытая (кабинеты и панель, noindex).
- **SEO-блок на каждой публичной странице** (123 из 123, проверено скриптом): заголовок, вводный абзац, «Читать полностью»
  с текстом и таблицей, заключение и **8 вопросов двумя колонками по 4**. Данные — `products/brands/directions/articles.json`
  (поля `seo`/`seoBlock`, `faq` ×8) и `seo_pages.json` для хабов и служебных страниц.
- **Вход и роли** (`tools/site/auth.js`): телефон + код (демо 4815), роли закупщик/поставщик/оператор, меню пользователя,
  выход, сброс демо-данных, защита `kabinet/`, `kabinet-postavshchika/`, `panel/`. Для проверок — `?demo=buyer|vendor|operator`.
- **Кабинет закупщика** (20 страниц, `gen_cabinet.js` + `cabinet.js`): заказы и карточки заказов, согласование по маршруту
  Цех → Бюджет → Закупка, регулярные закупки (лимит 50 000 ₽, порог цены 7 %), запросы КП, счета и УПД, спецификации,
  сотрудники, реквизиты — все действия работают и сохраняются.
- **Кабинет поставщика** (17 страниц, `gen_vendor.js` + `vendor.js`): заявки со сменой статусов и SLA-таймером, редактор
  прайса (правка в таблице, пакетно ±%, экспорт CSV), выгрузка фида с прогрессом и ошибками строк, расчёты, настройки.
- **Панель оператора** (21 новая страница + 9 оживлённых экранов, `gen_admin.js` + `admin.js`): CRM-канбан с перетаскиванием
  (мышь, тач, клавиатура), страницы сделок, клиенты, поставщики, очередь автопилота, матрица прав с двумя подписями,
  правила наценки CRUD, фиды, модерация отзывов, журнал изменений с откатом.
- Проверки: `check_site` — 0 проблем на 1440/390; все 71 закрытая страница под своей ролью — 0 ошибок и переполнений;
  обходчик ссылок — 0 битых (1 известное ложное срабатывание).
- Всё состояние кабинетов — в браузере пользователя (демо-режим подписан на страницах).

## Дополнено (этап 4, 15.09.2026) — перелинковка и иерархия заголовков
- **Заголовки** (`tools/seo_postprocess.js`, пост-обработка каждой страницы): ровно один h1, разделы h2, подразделы и карточки h3,
  0 пропусков уровней и 0 заголовков в шапке/подвале на всех 119 индексируемых страницах; вид страниц не изменился (сверено по геометрии 194 страниц).
- **Контекстные ссылки** в SEO-текстах, FAQ и статьях: 71 ссылка по точному имени бренда, направления, товара или статьи, не больше 5 на страницу, без ссылок на себя.
- **Блоки перелинковки** в шаблонах: «Ещё в разделе», «Смотрите также» (товар); «Смежные направления кластера», «Другие кластеры» (направление);
  «Направления с продукцией», «Другие производители» (бренд); «Что почитать дальше», «Подобрать оборудование» (статья); направления у брендов в хабе.
- **Карта сайта** — 118 ссылок: разделы, 45 направлений по кластерам, 16 брендов, 24 товара, 16 статей. Подвал ведёт на страницы направлений и хаб производителей.
- **Итог по индексируемым страницам:** входящих ссылок в среднем 24,6 → 34,6, у направлений 5,4 → 23,4 (минимум 7), сирот 0.
- Аудит структуры: `node tools/qa/seo_structure.js` (отчёт `docs/reports/2026-09-15-seo-structure.md`). Сборка берёт React из node_modules — не зависит от unpkg.

## Дополнено (этап 5, 15.09.2026) — каталоги категорий, скорость, финальная проверка
- **Каталоги 7 категорий** (`tools/gen_catalog.js` + `catalog.js`): арматура, приводы, подшипники, компрессоры, электрика,
  пневматика, запчасти — фильтры (производитель, подкатегория, наличие, цена), сортировка, `?tip=`, SEO-блок с 8 вопросами,
  JSON-LD BreadcrumbList + ItemList + FAQPage. Подкатегории направлений ведут в каталог с фильтром, у товаров крошка категории,
  кнопка «Открыть каталог» на направлениях, каталоги в карте сайта и sitemap (126 адресов).
- **Скорость** (`tools/perf_postprocess.js`): шрифты без блокировки отрисовки, повторяющиеся inline-стили → классы (−1,4 МБ HTML),
  минификация CSS. Lighthouse мобильный (с gzip): Performance в среднем 84 → 99,7, FCP 3,4–4,2 → 1,2–1,4 с, LCP 3,5–4,2 → 1,5–2,0 с;
  Accessibility 96–100, Best Practices и SEO 100. Вид страниц не изменился (сверено по геометрии).
- `aria-current="page"` у ссылок на текущую страницу; своё фото задвижки.
- **Итоговые проверки:** 201 страница; check_site — 0 проблем на 1440/390; структура заголовков — 0 нарушений на 126 индексируемых;
  обходчик ссылок — **0 ошибок**; сирот 0.

## Дополнено (15.09.2026) — шрифты со своего хостинга
- Fira Sans / Fira Sans Condensed из `@fontsource` (OFL): только кириллица + латиница и используемые начертания, 14 файлов woff2
  в `site/assets/fonts/`, `font-display: swap`. Запросов к Google Fonts больше нет.
- Lighthouse мобильный **на живом GitHub Pages**, до → после: главная 98→99, каталог 78→92, карточки 81/77→92, компрессоры 79→93,
  статья 78→91, производители 83→96, контакты 85→96, направления 98→87; **в среднем 84 → 93**. CLS главной 0,077 → 0,028.
  (Замеры агента на локальной копии давали ~99,7 — локальный сервер и живой CDN сравнивать напрямую нельзя.)

## Как пересобрать
```
node tools/build_site.js        # сборка site/ (~1 мин, атомарная)
node tools/check_site.js        # ссылки, мета, консоль, переполнение 1440/390
node tools/qa/crawl_links.js    # каждая карточка → своя страница
node tools/qa/seo_structure.js  # заголовки H1–H6, перелинковка, сироты
node tools/qa/lighthouse.js --serve site --gzip   # Lighthouse
git push                         # GitHub Pages публикует site/ (workflow pages.yml)
```
Данные новых страниц — `tools/data/*.json`, шаблоны — `tools/gen_pages.js`, экраны — `project/*.dc.html`.

## Что осталось
- Отзывы и рейтинг есть только у Grundfos CR 32-4 (без реальных данных не добавляем).
- Подкатегории направлений (кроме «Насосов») ведут в чат с запросом — отдельных каталогов по ним нет.
- Кабинеты и панель работают без сервера: данные в localStorage браузера; для реальной работы нужен бэкенд (API, авторизация, БД).

## Что нужно от Александра
- Домен (сейчас github.io), Яндекс Вебмастер / Search Console, Метрика.
- Реквизиты юрлица, телефон, настоящие почты вместо `*.example`.
- Официальные SVG-логотипы брендов (если нужны настоящие знаки в бегущей строке).

## Отчёты агентов
`docs/reports/2026-09-14-*.md`: audit-maketnost, mobile-ux, seo-audit, qa-links, qa-templates, perf, a11y-data.
